// [in-dev: blaz] File currently open


//
// Examples ( guest starring variables BigDataset, SmallDataStructure ):
//
// var args = { filter: { type: 'something'} }
// args.data = 
// findIn( args )
//
//

const inOnion = (args) => {

	var { }
	var pointersArr = [],
			matchDelimiters = /(?=\.|\#)/g,

	while ((match = matchDelimiters.exec(pointersStr)) != null) {
	  pointersArr.push(
	  	match.trim() 
	  )
	}
}

const peelObj = ( pointersArr, objOnion, mergeWith ) => {

	var unwrap = pointersArr.shift(), // returns first item, removes it from origin array
			type = toUnwrap.charAt(0),
			unwrap = unwrap.substr(1)
			err = null
	

	if( '#' == type &&
			'undefined' !== typeof objOnion[ ]){
		objOnion
	}

	if( '.' == )


	return 
	( !eetoUnwrap && pointersArr.length > 0 ) ? 
		findInObj( pointersArr, objOnion[ toUnwrap ] )
	: 
}